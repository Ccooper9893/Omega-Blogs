<section class="row d-flex justify-content-center">
    <div class="card">
  <div class="card-body">
    <h4>Welcome to your Omega Dashboard {{username}}</h4>
  </div>
</div>
</section>
<section class="row d-flex justify-content-center">
    {{#each blogs as |blog|}}
      {{> user-blogs}}
    {{/each}}
    </section>